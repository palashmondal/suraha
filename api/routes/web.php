<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Web routes
|--------------------------------------------------------------------------
| Suraha's API serves JSON only — the Bangla SPA is a separate Vite build in
| web/, put in front of this by Caddy (see infra/Caddyfile). There is no
| server-rendered page here, so this file is intentionally empty; the health
| check lives at /up, registered in bootstrap/app.php.
*/
