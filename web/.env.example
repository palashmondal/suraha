# Base URL of the Suraha API. When unset, the PWA runs in demo mode (mock sessions, any 6-digit OTP)
# so the client is usable before the backend is running. Point it at the Laravel API to go live:
# VITE_API_URL=http://localhost:8000/api
VITE_API_URL=
